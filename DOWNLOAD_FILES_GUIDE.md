# 쌤 도구함 다운로드 자료 가이드

쌤 도구함에서 **다운로드 타입** 도구가 정상 동작하려면 실제 파일이 있어야 합니다.

---

## 📁 파일 위치

- **폴더**: `assets/downloads/`
- **경로 예**: `assets/downloads/상담일지양식.xlsx`

`editions-data.js`의 각 호 `tools[]` 안에서 `type: 'download'`인 항목의 `url`이 이 경로를 가리키면, 해당 파일이 있을 때만 다운로드됩니다.

---

## 📋 현재 사용 중인 파일명 (예시)

| 파일명 | 용도 (editions-data.js 기준) |
|--------|------------------------------|
| `상담일지양식.xlsx` | 상담 일지 양식 |
| `수업준비체크리스트.pdf` | 수업 준비 체크리스트 |
| `견적요청서.xlsx` | 견적 요청서 양식 |

새 도구를 추가할 때는 **url**에 넣은 파일명과 **assets/downloads/** 안의 실제 파일명이 **완전히 같아야** 합니다.

---

## ✅ 준비 절차

1. **파일 준비**: 엑셀/PDF 등으로 양식을 만듭니다.
2. **이동**: 해당 파일을 프로젝트의 **`assets/downloads/`** 폴더에 복사합니다.
3. **이름 확인**: `editions-data.js`의 `tools[].url`에 적힌 파일명과 동일한지 확인합니다.
   - 예: `url: "assets/downloads/상담일지양식.xlsx"` → 파일명은 `상담일지양식.xlsx`
4. **배포**: Netlify 등에 배포할 때 `assets/downloads/` 폴더가 함께 올라가는지 확인합니다.

---

## ⚠️ 파일이 없을 때

- **파일이 없으면** 사용자가 다운로드 버튼을 눌렀을 때 **다운로드되지 않고**,  
  **「"○○" 자료는 준비 중입니다…」** 안내만 표시됩니다.
- 404 에러 페이지가 파일처럼 저장되는 현상은 방지되어 있습니다.

---

## 🔗 관련 문서

- **도구/매거진 데이터 추가**: `TOOLS_AND_ARTICLES_GUIDE.md`
- **발행분 추가 전체**: `HOW_TO_ADD_EDITION.md`
