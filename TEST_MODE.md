# 테스트 모드 (개발용)

개발·검증 시 사용하는 테스트 모드 안내입니다.

## 활성화 방법

메인 페이지 URL에 다음 파라미터를 붙입니다.

- **`?test=1`** 또는 **`?test=true`**

예시:

- 로컬: `index.html?test=1` 또는 `http://localhost:8888/?test=1`
- 배포: `https://weekly-nexo.netlify.app/?test=1`

## 테스트 모드에서 바뀌는 것

1. **상단 배너**  
   "🧪 테스트 모드 · 개발용" 배너가 페이지 최상단에 표시됩니다.

2. **검색**  
   리뉴얼 후에는 **검색 UI가 항상** 헤더 아래에 노출됩니다. 테스트 모드가 아니어도 사용할 수 있습니다.

## 함께 쓰면 좋은 파라미터

- **미리보기 모드**: `?test=1&preview=1`  
  발행일이 지나지 않은 호도 선택·본문 확인 가능 (관리자 미리보기와 동일).

- **특정 호**: `?test=1&edition=2026-02-12`  
  테스트 모드로 해당 호를 바로 엽니다.

## 주의

- 테스트 모드용 URL(`?test=1`)은 **실서비스 공유용으로 쓰지 마세요.**  
- 배포 환경에서 테스트할 때만 사용하고, 일반 사용자에게는 노출되지 않도록 합니다.
